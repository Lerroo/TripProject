@model FastTripApp.Web.Models.CreateTripViewModel

<div class="border p-3">
    <div class="form-group row">
        <h2 class="text-info pl-3">Create new Fast Trip</h2>
    </div>

    <div class="row">
        <div class="col">
            <form asp-action="Create" method="post">
                @await Html.PartialAsync("_FormBody", Model)

                <div class="row d-flex justify-content-end">
                    <div class="col-8 offset-4 row">

                        <div class="col">
                            <input type="submit" class="btn btn-info w-100" value="Create" />
                        </div>
                        <div class="col">
                            <a asp-action="Index" class="btn btn-success w-100"><i class="fas fa-sign-out-alt"></i> Back</a>
                        </div>

                    </div>
                </div>
            </form>
        </div>
        <div class="col-5 text-right mx-auto" id="map"> </div>
    </div>

</div>

<div id="image_div">
    <img id="googleStaticPicture" />
</div>
